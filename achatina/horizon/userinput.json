{
    "services": [
        {
            "org": "$HZN_ORG_ID",
            "url": "$SERVICE_NAME",
            "variables": {
                "KAFKA_BROKER_URLS": "$KAFKA_BROKER_URLS",
                "KAFKA_API_KEY": "$KAFKA_API_KEY",
                "KAFKA_PUB_TOPIC": "$KAFKA_PUB_TOPIC",
                "INPUT_URL": "$INPUT_URL",
                "NODE": "$NODE",
                "HOST_IP": "$HOST_IP"
            }
        }
    ]
}